/**
 * Canonical browser entry: same WASM (`pkg/`), in-memory and OPFS persistence APIs.
 * Implementation modules: `wrapper.js`, `opfs-wrapper.js`, `opfs-worker.js`.
 */
export * from './wrapper.js';
export * from './opfs-wrapper.js';
