{
  "name": "mongosh-service-provider",
  "version": "0.0.0",
  "description": "MongoDB Shell Service Provider Package",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive --color",
    "test-ci": "mocha --recursive",
    "check": "mongodb-js-precommit './lib/**/*{.js}' './test/**/*.js'"
  },
  "dependencies": {
    "mongosh-transport-server": "file:../transport-server"
  }
}
