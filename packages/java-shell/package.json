{
  "name": "java-integration",
  "version": "1.0.0",
  "description": "Java application that uses mongosh-shell-api node module and MongoDB java driver to run mongo shell on JVM",
  "scripts": {
    "browserify-mapper": "browserify src/main/js/all.js -o src/main/resources/js/all-standalone.js -dv"
  },
  "engines": {
    "node": "^12.4.0"
  }
}
