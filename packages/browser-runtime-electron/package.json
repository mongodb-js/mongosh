{
  "name": "@mongosh/browser-runtime-electron",
  "version": "5.1.1",
  "description": "Mongosh browser runtime electron",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "engines": {
    "node": ">=14.15.1"
  },
  "config": {
    "unsafe-perm": true
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/mongodb-js/mongosh.git"
  },
  "scripts": {
    "test": "mocha",
    "test-ci": "node ../../scripts/run-if-package-requested.js pnpm test",
    "test-coverage": "nyc --no-clean --cwd ../.. --reporter=none pnpm run test",
    "test-ci-coverage": "nyc --no-clean --cwd ../.. --reporter=none pnpm run test-ci",
    "eslint": "eslint",
    "lint": "pnpm run eslint . && pnpm run prettier -- --check .",
    "check": "pnpm run lint",
    "preprepublish": "rimraf ./lib",
    "prepublish": "pnpm run compile",
    "compile": "tsc -p tsconfig.json",
    "prettier": "prettier",
    "reformat": "pnpm run prettier -- --write . && pnpm run eslint -- --fix"
  },
  "author": "",
  "license": "Apache-2.0",
  "publishConfig": {
    "access": "public"
  },
  "mongosh": {
    "unitTestsOnly": true
  },
  "devDependencies": {
    "@mongodb-js/eslint-config-mongosh": "^1.0.0",
    "@mongodb-js/prettier-config-devtools": "^1.0.1",
    "@mongodb-js/tsconfig-mongosh": "^1.0.0",
    "@mongosh/service-provider-node-driver": "^5.0.2",
    "bson": "^7.2.0",
    "eslint": "^7.25.0",
    "prettier": "^2.8.8",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@mongosh/browser-runtime-core": "^5.1.1",
    "@mongosh/service-provider-core": "5.0.1",
    "@mongosh/types": "^5.0.1"
  }
}
