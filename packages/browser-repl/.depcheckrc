ignores: [
    # used by less-loader
    'less',
    # used by webpack
    'webpack-cli',
    # resolved as `<depname>/` so depcheck doesn't see it being used
    'buffer',
    'util',
    # leafygreen peer dep
    '@emotion/react'
  ]
skip-missing: true
